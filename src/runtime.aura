use (syscall_exit, syscall_write, string_into) = "core";

def write(fd: Int, bytes: Bytes) -> Void {
    syscall_write(fd, bytes);
    ()
}

def bytes_from_string(text: String) -> Bytes {
    string_into(text)
}

def exit_process(code: Int) -> Void {
    syscall_exit(code)
}
